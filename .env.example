# Copy this file to .env and adjust if your backend runs somewhere other
# than the default. Never put AI provider keys here — those belong only in
# backend/.env, since anything prefixed VITE_ is bundled into the public
# client build.
VITE_API_URL=http://localhost:8000/api
